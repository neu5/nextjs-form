export type User = {
  id: string;
  name: string;
  email: string;
  password: string;
};

export type GroupsTable = {
  id: string;
  name: string;
  pathname: string;
  date: string;
};

export type PathForm = {
  id: string;
  name: string;
};

export type PathsField = {
  id: string;
  name: string;
};
